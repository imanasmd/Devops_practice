pipeline{
    agent any
        stages{
            stage('code-checkout'){
                steps{
                    echo "Running ${env.BUILD_ID} on ${env.JENKINS_URL}"
                }
                }
        }
}
